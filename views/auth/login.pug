-
  __status.errorText = {
    error: 'Please verify if your username/password is correct.'
  }[__status.error];

doctype html
html(lang='en')
  head
    title Login | UpRoar
    include /global/head
  body
    include /global/header
    .container.center
      h1 Login
      .flex-row
        form(method='post')
          table.table-layout-form.table-style-default
            tr
              td
                label(for='username') Username
              td
                input(type='text' name='username' required)
            tr
              td
                label(for='password') Password
              td
                input(type='password' name='password' required)
            //- tr
            //-   td
            //-     input(type='checkbox' name='remember_me')
            //-     label Remember Me
          button.btn.btn-green(type='submit') Login
      a.link(href='/auth/sign-up') Don't have an account? Sign Up
      br
      a.link.error(href='#') Forgot username/password?